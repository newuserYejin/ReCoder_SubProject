<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ohgiraffers.refactorial.mail.model.dao.MailEmployeeMapper">

    <!-- 회원 이름으로 검색 -->
    <select id="searchMailEmployeesByName" resultType="com.ohgiraffers.refactorial.mail.model.dto.MailEmployeeDTO">
        SELECT
        e.*, d.dept_name, p.position_name
        FROM
        employee e
        JOIN tbl_dept d ON e.dept_code = d.dept_code
        JOIN tbl_position p ON e.position_value = p.position_value
        WHERE
        emp_name LIKE CONCAT('%', #{searchReceiverInput}, '%')
    </select>

    <!-- 전체 직원 조회 -->
    <select id="getAllMailEmployees" resultType="com.ohgiraffers.refactorial.mail.model.dto.MailEmployeeDTO">
        SELECT
        e.*, d.dept_name, p.position_name
        FROM
        employee e
        JOIN tbl_dept d ON e.dept_code = d.dept_code
        JOIN tbl_position p ON e.position_value = p.position_value
    </select>
</mapper>
